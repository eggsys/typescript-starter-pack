{
  "name": "practice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "check1": "ts-node-dev --respawn --transpile-only src/object/check1.ts",
    "array": "ts-node-dev --respawn --transpile-only src/array-method/array-demo.ts",
    "class": "ts-node-dev --respawn --transpile-only src/class/server.ts",
    "class2": "ts-node-dev --respawn --transpile-only src/class2/server.ts",
    "object": "ts-node-dev --respawn --transpile-only src/test/server.ts",
    "backoff": "ts-node-dev --respawn --transpile-only src/backoff/server.ts",
    "test:unit": "jest unit --detectOpenHandles --forceExit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^29.4.1",
    "ts-jest": "^29.0.5",
    "typescript": "^4.6.2"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "ts-node-dev": "^1.1.8"
  }
}
